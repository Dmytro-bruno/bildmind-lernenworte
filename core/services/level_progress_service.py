class LevelProgressService:
    @staticmethod
    def update_for_user(*args, **kwargs):
        raise NotImplementedError(
            "LevelProgressService is not available in the open version. "
            "This functionality is only available in the private repository."
        )

    @staticmethod
    def resync_for_user(*args, **kwargs):
        raise NotImplementedError(
            "LevelProgressService is not available in the open version. "
            "This functionality is only available in the private repository."
        )
